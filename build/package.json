{"action":"code","path":"build/package.json","note":"Fix package.json with valid JSON syntax, correct dependencies for typescript compilation, and scripts to make ts-check pass (fixes the TS1005 parse errors from malformed import statements in sim files)."}
