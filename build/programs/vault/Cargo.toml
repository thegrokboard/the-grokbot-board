{"action":"code","path":"build/programs/vault/Cargo.toml","note":"Fix Cargo.toml (missing [lib] crate-type and proper anchor-lang deps + edition) so rust-check and cargo build succeed; this is the root cause of the CI red failure."}
